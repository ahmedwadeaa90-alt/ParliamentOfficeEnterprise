<Window x:Class="ParliamentOffice.UI.Views.CitizensWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إدارة المواطنين"
        Height="720"
        Width="1100"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft"
        FontFamily="Segoe UI"
        Background="#F4F6F8">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="إدارة المواطنين والأضابير"
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="0,0,0,14"/>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="14" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="FullNameBox" Grid.Column="0" Margin="6" Height="34" />
                <TextBox x:Name="PhoneBox" Grid.Column="1" Margin="6" Height="34" />
                <TextBox x:Name="AddressBox" Grid.Column="2" Margin="6" Height="34" />
                <TextBox x:Name="NotesBox" Grid.Column="3" Margin="6" Height="34" />

                <StackPanel Grid.Column="4" Orientation="Horizontal">
                    <Button x:Name="SaveCitizenButton" Content="حفظ" Width="80" Margin="6" Height="34"/>
                    <Button x:Name="UpdateCitizenButton" Content="تعديل" Width="80" Margin="6" Height="34"/>
                    <Button x:Name="DeleteCitizenButton" Content="حذف" Width="80" Margin="6" Height="34"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBox x:Name="SearchBox"
                     Height="36"
                     Margin="0,0,0,10"
                     FontSize="15"/>

            <DataGrid x:Name="CitizensGrid"
                      Grid.Row="1"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      Background="White"
                      HeadersVisibility="Column"
                      RowHeight="34">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="التسلسل" Binding="{Binding Id}" Width="80"/>
                    <DataGridTextColumn Header="اسم المواطن" Binding="{Binding FullName}" Width="2*"/>
                    <DataGridTextColumn Header="الهاتف" Binding="{Binding Phone}" Width="*"/>
                    <DataGridTextColumn Header="العنوان" Binding="{Binding Address}" Width="2*"/>
                    <DataGridTextColumn Header="الملاحظات" Binding="{Binding Notes}" Width="2*"/>
                    <DataGridTextColumn Header="تاريخ الإدخال" Binding="{Binding CreatedAt}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>