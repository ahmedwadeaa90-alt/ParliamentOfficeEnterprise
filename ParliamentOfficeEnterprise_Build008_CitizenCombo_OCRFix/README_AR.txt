Parliament Office Enterprise - Build007
======================================

طريقة التشغيل:
1) افتح ParliamentOfficeEnterprise.sln في Visual Studio.
2) شغّل مشروع ParliamentOffice.UI.
3) بيانات الدخول الافتراضية:
   admin
   1234

ملاحظات Build007:
- إصلاح ترتيب إنشاء قاعدة البيانات: يتم إضافة الأعمدة الناقصة قبل إنشاء الفهارس.
- إصلاح خطأ no such column: CitizenId.
- إضافة Migration تلقائي يحافظ على قاعدة البيانات القديمة بدون حذف.
- إنشاء نسخة احتياطية تلقائية قبل ترقية القاعدة داخل Documents\ParliamentOfficeEnterprise\Backups.
- إصلاح حساب المدير تلقائياً عند التشغيل.
- إضافة سجل خطأ عند فشل التشغيل داخل Documents\ParliamentOfficeEnterprise\Logs.
- الصادر/الوارد يدعمان المواطن، المرفقات، التعديل والحذف للمدير فقط.
- التقارير HTML موجودة داخل تبويب التقارير.

مهم:
لا تحذف مجلد Documents\ParliamentOfficeEnterprise عند التحديث إلا إذا أردت بدء قاعدة جديدة تماماً.


Build007:
- إصلاح التقرير الشامل ORDER BY.
- إخفاء أعمدة Id/CitizenId من الجداول.
- تحسين ربط المواطن بالصادر والوارد عند تحديد السجل.
- تبسيط أزرار التعديل والحذف والإرفاق.
- تحسين رسالة OCR وإتاحة قراءة TXT للاختبار.
